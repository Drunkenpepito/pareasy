<%= render 'shared/onglet' %>
<div class = "container-select-event">
<!--   <div class = "arrow-left">
    <%#= link_to "", edit_description_event_path(@event), class:"fas fa-arrow-left", data: {turbolinks: false} %>
  </div> -->
  <h2>Récapitulatif du Pari</h2>
</div>

<div class = "main-container">
        <div class="cardsEvent">
          <div class="card-event event-<%= @event.sport %>">
            <br>
            <div class="card-event-bet" id = "match">
              <p><%=@event.game %></p>
            </div>
            <div class="card-event-bet">
              <p><%=@event.bet_type %></p>
            </div>
            <div class="card-event-bet">
              <p><%=@event.description %></p>
            </div>
          </div>
        </div>

  <h2>Choisis la mise</h2>
  <%#= simple_form_for [@event, @bet] do |f| %>
    <%#= f.input :amount_cents, label: false, placeholder: "Combien souhaites-tu miser ?", input_html: { min: '0', max: '10' } %>
    <%#= f.input :result, as: :boolean, checked_value: true, unchecked_value: false %>
    <%#= f.button :submit, "Next", class: "btn-flat" %>
  <%# end %>
  <%# raise %>
<!-- </div> -->


  <%= simple_form_for @bet, url: event_bets_path(@event), html: { method: :post } do |f| %>
    <%= f.hidden_field :amount_cents %>
    <div class = "container-description">
      <div data-event="1" class="list-description-bet">
        1€
      </div>
      <div data-event="2" class="list-description-bet">
        2€
      </div>
      <div data-event="4" class="list-description-bet">
        4€
      </div>
      <div data-event="6" class="list-description-bet">
        6€
      </div>
       <div data-event="8" class="list-description-bet">
        8€
      </div>
       <div data-event="10" class="list-description-bet">
        10€
      </div>
      <% f.button :submit, "Next" %>
    </div>
  <% end %>
